name: Publish GHCR Image

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - routes-api

permissions:
  contents: read
  packages: write

jobs:
  publish:
    name: Build and Push Image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Resolve image name and tags
        id: vars
        run: |
          owner="$(echo "$GITHUB_REPOSITORY_OWNER" | tr '[:upper:]' '[:lower:]')"
          repo="$(basename "$GITHUB_REPOSITORY" | tr '[:upper:]' '[:lower:]')"
          image="ghcr.io/${owner}/${repo}"
          echo "image=${image}" >> "$GITHUB_OUTPUT"

          branch="${GITHUB_REF#refs/heads/}"
          echo "branch=${branch}" >> "$GITHUB_OUTPUT"

          # Build the tag list. Both main and routes-api get :latest
          # so production can always pull :latest regardless of branch.
          echo 'tags<<EOF' >> "$GITHUB_OUTPUT"
          echo "${image}:${branch}" >> "$GITHUB_OUTPUT"
          echo "${image}:latest" >> "$GITHUB_OUTPUT"
          echo "${image}:sha-${{ github.sha }}" >> "$GITHUB_OUTPUT"
          echo 'EOF' >> "$GITHUB_OUTPUT"

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          target: full
          push: true
          platforms: linux/amd64
          build-args: |
            BRANCH=${{ steps.vars.outputs.branch }}
          tags: ${{ steps.vars.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
